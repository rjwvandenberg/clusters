# yaml-language-server: $schema=https://raw.githubusercontent.com/ansible/ansible-lint/main/src/ansiblelint/schemas/role-arg-spec.json
argument_specs:
  main:
    short_description: Setup kubernetes prerequisites
    options:
      root_certificate:
        type: str
        default: "Unspecified"
      reboot:
        type: bool
        default: true